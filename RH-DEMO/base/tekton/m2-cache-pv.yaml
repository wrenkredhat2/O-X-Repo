apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: maven-m2-pvc
spec:
  accessModes:
    - ReadWriteOnce  # Or ReadWriteMany if your provider supports it
  resources:
    requests:
      storage: 5Gi   # Give it enough room for all those Quarkus dependencies