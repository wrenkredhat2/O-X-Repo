apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: keycloak

resources:
  - rolebinding.yaml
  - ../../../keycloak/base

patches:

  - target:
      kind: Keycloak
      name: orlen-int-kc
    patch: |-
      - op: replace
        path: /spec/hostname/hostname
        value: "keycloak.apps.cluster-492gk.492gk.sandbox2076.opentlc.com"